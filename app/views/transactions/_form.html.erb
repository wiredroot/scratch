<%= form_for @transaction do |f| %>
  <% if @transaction.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@transaction.errors.count, "error") %> prohibited
        this Transaction from being saved:
      </h2>
      <ul>
        <% @transaction.errors.full_messages.each do |transaction| %>
          <li><%= transaction %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label "Person:" %> <%= f.text_field :profile_id %>
  </p>

  <p>
    <%= f.label "Stock:" %> <%= f.text_field :stock_id %>
  </p>

  <p>
    <%= f.label "Shares:" %> <%= f.text_field :shares %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>